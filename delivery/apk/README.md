# Placeholder APK

Letakkan APK final di folder ini sebelum pengumpulan:

```text
lost-found-campus-release.apk
```

Output build yang dihasilkan Flutter:

```text
mobile/build/app/outputs/flutter-apk/app-release.apk
```

APK tidak disertakan secara default karena file release biasanya besar dan perlu dibuat ulang dengan URL API produksi final.
